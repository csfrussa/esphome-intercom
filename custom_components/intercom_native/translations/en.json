{
  "config": {
    "step": {
      "user": {
        "title": "Intercom Native",
        "description": "SIP softphone and SIP bridge/router for ESPHome VoIP phones.",
        "data": {
          "sip_tcp_enabled": "Enable SIP TCP",
          "sip_udp_enabled": "Enable SIP UDP",
          "sip_port": "SIP port",
          "rtp_port": "RTP port",
          "advertise_host": "Advertised HA host/IP",
          "assist_intents": "Enable Assist intents"
        },
        "data_description": {
          "sip_tcp_enabled": "Listen for SIP signaling over TCP. Enabled by default for HA softphone and PBX routing.",
          "sip_udp_enabled": "Listen for SIP signaling over UDP. Enable this when ESP devices or external softphones call HA over SIP UDP.",
          "sip_port": "Listen port for the HA SIP endpoint.",
          "rtp_port": "Base RTP UDP port for HA media and relays.",
          "advertise_host": "Optional override published in ESP phonebooks. Use this when HA is behind routing, NAT, LXC, VPN, or multiple subnets and the automatically announced address is not reachable by ESP devices.",
          "assist_intents": "Register optional Assist handlers for call, hang up, answer and decline (\"Hey Jarvis, call Kitchen\"). Spoken contact names are resolved dynamically against the live intercom phonebook."
        }
      }
    },
    "error": {
      "sip_transport_required": "At least one SIP signaling transport must be enabled."
    },
    "abort": {
      "reconfigure_successful": "[%key:common::config_flow::abort::reconfigure_successful%]",
      "already_configured": "Intercom Native is already configured."
    }
  }
}
