# Virtual Profiles

This directory is populated by:

```bash
python3 tools/simulator/generate_virtual_profiles.py
```

Do not hand-maintain generated YAML files here. Phase 00V requires virtual
profiles to be derived from real profile metadata so the simulator cannot drift
from firmware packages.
